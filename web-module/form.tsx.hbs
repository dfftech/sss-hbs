"use client";

import { useSignals } from "@preact/signals-react/runtime";
import { ArticleLayout } from "@/layouts/article-layout";
import { ContentLayout } from "@/layouts/content-layout";
import { TypeInput } from "@/types/type.input";
import { TypeSwitch } from "@/types/type.switch";
import { TypeButton } from "@/types/type.button";
import { useTranslation } from "react-i18next";
import { use{{to_pascal_case name}}Form } from "./{{to_kebab_case name}}.form.ts";

export function {{to_pascal_case name}}Form() {
  useSignals();
  const { t } = useTranslation();
  const { nameProps, activeProps, cancelProps, saveProps } = use{{to_pascal_case name}}Form();
  return (
    <section className="w-full">
      <ArticleLayout>
        <div className="flex justify-between gap-4">
          <h3 className="text-xl font-bold text-primary">{t("{{to_camel_case name}}Form")}</h3>
          <div className="flex gap-2">
            <TypeButton {...cancelProps} />
            <TypeButton {...saveProps} />
          </div>
        </div>
      </ArticleLayout>
      <ContentLayout>
        <div className="p-4">
          <TypeInput {...nameProps} />
          <TypeSwitch {...activeProps} />
        </div>
      </ContentLayout>
    </section>
  );
}
