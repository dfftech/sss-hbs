"use client";

import { useSignals } from "@preact/signals-react/runtime";
import { ArticleLayout } from "@/layouts/article-layout";
import { GridLayout } from "@/layouts/grid-layout";
import { FloatLayout } from "@/layouts/float-layout";
import { TypeSearch } from "@/types/type.search";
import { TypeSwitch } from "@/types/type.switch";
import { TypeButton } from "@/types/type.button";
import { use{{to_pascal_case name}}List } from "./{{to_kebab_case name}}.list.ts";

export function {{to_pascal_case name}}List() {
  useSignals();
  const { t, searchProps, {{to_camel_case name}}StatusProps, reloadProps, addProps } =
    use{{to_pascal_case name}}List();

  return (
    <>
      <ArticleLayout>
        <aside className="flex justify-between gap-2">
          <h2 className="text-2xl font-bold text-primary">{t("{{to_camel_case name}}")}</h2>
          <div className="flex gap-4">
            <TypeSearch {...searchProps} />
            <TypeSwitch {... {{to_camel_case name}}StatusProps} />
          </div>
        </aside>
      </ArticleLayout>
      <GridLayout>Load List</GridLayout>
      <FloatLayout>
        <div className="flex gap-2">
          <TypeButton {...reloadProps} />
          <TypeButton {...addProps} />
        </div>
      </FloatLayout>
    </>
  );
}
