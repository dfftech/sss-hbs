apiVersion: v1
kind: Secret
metadata:
  name: {{to_kebab_case tenant}}-cred
  namespace: {{to_kebab_case tenant}}-{{addon.ns}}
data:
  .dockerconfigjson: "{{addon.data}}"
type: kubernetes.io/dockerconfigjson
