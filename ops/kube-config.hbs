apiVersion: v1
kind: ConfigMap
metadata:
  name: {{module}}-config
  namespace: {{tenant}}-{{addon.ns}}
data:
  {{constantCase module}}_DB: "{{addon.db}}"
  {{constantCase module}}_BASE_URL: "{{addon.url}}"
