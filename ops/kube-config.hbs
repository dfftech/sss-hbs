apiVersion: v1
kind: ConfigMap
metadata:
  name: {{to_kebab_case module}}-config
  namespace: {{to_kebab_case tenant}}-{{addon.ns}}
data:
  {{to_screaming_snake_case module}}_DB: "{{addon.db}}"
  {{to_screaming_snake_case module}}_BASE_URL: "{{addon.url}}"
