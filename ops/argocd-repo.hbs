apiVersion: v1
kind: Secret
metadata:
  name: {{tenant}}-{{module}}.repo
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  project: default
  name: {{tenant}}-{{module}}.repo
  url: https://gitlab.com/{{tenant}}-{{module}}/{{module}}-cd.git
  username: {{addon.user}}
  password: {{addon.pass}}
