curl -X PUT 'https://{{addon.url}}/apisix/admin/routes/{{module}}-{{name}}-{{type}}' \
    -H 'X-API-KEY: {{token}}' \
    -H 'Content-Type: application/json' \
    -d ' {
        "uri": "/{{module}}-{{name}}/*",
        "name": "{{module}}-{{name}}-{{type}}",
        "plugins": {
            "jwe-decrypt": {
             "header": "authorization",
             "forward_header": "session-info"
           },
           "proxy-rewrite": {
             "regex_uri": [
               "^/{{module}}-{{name}}/(.*)",
               "/$1"
              ]
            }
        },
        "upstream": {
          "nodes": [
            {
              "host": "{{module}}-{{name}}-{{type}}-svc.{{tenant}}-{{addon.ns}}.svc.cluster.local",
              "port": 80,
              "weight": 1
            }
          ],
          "type": "none",
          "scheme": "http"
        },
        "status": 1
    }'
