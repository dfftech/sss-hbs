export const ContentLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <main className="flex-grow shadow-md mx-auto w-full overflow-y-auto bg-base-200 rounded-none">
      {children}
    </main>
  );
};
