'use client';

import React from 'react';

export const SkeletonMenuManagementList: React.FC = () => {
    return (
        <div className='flex-1 p-4 flex flex-col gap-3'>
            {[1, 2, 3, 4, 5, 6].map((i) => (
                <div key={i} className='h-12 w-full bg-gray-300 dark:bg-gray-700 rounded animate-pulse' />
            ))}
        </div>
    );
};

export const SkeletonSubMenuList: React.FC = () => {
    return (
        <div className='flex-1 p-4 flex flex-col gap-3'>
            {[1, 2, 3, 4].map((i) => (
                <div key={i} className='h-16 w-full bg-gray-300 dark:bg-gray-700 rounded animate-pulse' />
            ))}
        </div>
    );
};

export const SkeletonMenuManagement: React.FC = () => {
    return (
        <div className='flex-1 flex gap-6 overflow-hidden'>
            {/* Left Panel Skeleton */}
            <div className='w-1/2 bg-gray-100 dark:bg-gray-800 rounded-lg shadow-sm border border-gray-300 dark:border-gray-700 flex flex-col'>
                <SkeletonMenuManagementList />
            </div>

            {/* Right Panel Skeleton */}
            <div className='w-1/2 bg-gray-100 dark:bg-gray-800 rounded-lg shadow-sm border border-gray-300 dark:border-gray-700 flex flex-col'>
                <SkeletonSubMenuList />
            </div>
        </div>
    );
};
